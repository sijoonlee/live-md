node_modules/
dist/
.DS_Store
test-results/
playwright-report/
data/*.sqlite
data/images/
data/blobs/
.playwright-data/
.env
playwright/.auth/
# The extension has no dependencies of its own — the root project builds it — so a
# lockfile here is vestigial (npx/vsce leave one behind).
vscode-extension/package-lock.json
