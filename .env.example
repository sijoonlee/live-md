# Where the SQLite database and file blobs live.
DATA_DIR=data

# Port to listen on. The host is always 127.0.0.1: this build has no
# authentication, so it refuses to start bound to anything else.
PORT=3000
